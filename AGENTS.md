# model-routing — agent instructions

`CLAUDE.md` in this directory is the primary agent instructions file for this repo. **Read it** — it holds the project framing and the `## Agent skills` configuration (triage labels, domain docs).

This file is a pointer only, so the two can't drift. Don't duplicate content here.
